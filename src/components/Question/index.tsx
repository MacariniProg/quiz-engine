export const Question = () => {
  // Each slide, apart from the question itself, can have an image, title, and description
  return (
    <div>
      <h1 className="title">Question</h1>
    </div>
  )
}